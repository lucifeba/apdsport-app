import type{AgentEnv,Analysis,Source}from'./client-types';import{now,num,yes,hash,checkpoint,setCheckpoint}from'./client-utils';import{sources}from'./client-google';import{analyze,priority}from'./client-analysis';import{publish,alert}from'./client-outputs';
async function previous(e:AgentEnv,ref:string,key:string){const r=await e.DB.prepare('SELECT source_key,analysis_json FROM sources WHERE client_ref=? AND source_key<>? ORDER BY processed_at DESC LIMIT 1').bind(ref,key).first<{source_key:string;analysis_json:string}>();if(!r)return null;try{return{key:r.source_key,a:JSON.parse(r.analysis_json)as Analysis}}catch{return null}}
async function handle(e:AgentEnv,t:string,s:Source){const key=`${s.sourceType}:${s.id}`,contentHash=await hash(s.text),dry=yes(e.DRY_RUN,true),old=await e.DB.prepare('SELECT revision,content_hash,status FROM sources WHERE source_key=?').bind(key).first<{revision:string;content_hash:string;status:string}>();if(old&&old.revision===s.revision&&old.content_hash===contentHash&&(dry||old.status!=='SIMULADO'))return'skipped';const ref=`CP-${(await hash(s.clientSeed)).slice(0,8).toUpperCase()}`,prev=await previous(e,ref,key),a=await analyze(e,s,prev?.a||null),pr=priority(s,a),at=now();let fileId='',url='SIMULACIÓN',channel='',ts='',notionId='';if(!dry){const x=await publish(e,t,s,ref,a,pr.label,pr.score,await hash(key));fileId=x.fileId;url=x.url;channel=x.channel;ts=x.ts;notionId=x.notionId}await e.DB.prepare(`INSERT INTO sources(source_key,source_type,external_id,revision,modified_at,content_hash,title_hash,mime_type,client_ref,status,priority,priority_score,material_type,draft_file_id,draft_url,slack_channel,slack_ts,notion_page_id,analysis_json,metadata_json,previous_source_key,processed_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?) ON CONFLICT(source_key) DO UPDATE SET revision=excluded.revision,modified_at=excluded.modified_at,content_hash=excluded.content_hash,title_hash=excluded.title_hash,mime_type=excluded.mime_type,client_ref=excluded.client_ref,status=excluded.status,priority=excluded.priority,priority_score=excluded.priority_score,material_type=excluded.material_type,draft_file_id=excluded.draft_file_id,draft_url=excluded.draft_url,slack_channel=excluded.slack_channel,slack_ts=excluded.slack_ts,notion_page_id=excluded.notion_page_id,analysis_json=excluded.analysis_json,metadata_json=excluded.metadata_json,previous_source_key=excluded.previous_source_key,processed_at=excluded.processed_at,updated_at=excluded.updated_at`).bind(key,s.sourceType,s.id,s.revision,s.modifiedAt,contentHash,await hash(s.title),s.mimeType,ref,dry?'SIMULADO':'PENDIENTE',pr.label,pr.score,a.material_type,fileId,url,channel,ts,notionId,JSON.stringify(a),JSON.stringify(s.metadata),prev?.key||null,at,at).run();return'processed'}
export async function run(e:AgentEnv,trigger:string){const started=now(),ins=await e.DB.prepare('INSERT INTO runs(trigger_type,started_at,status) VALUES(?,?,?)').bind(trigger,started,'RUNNING').run(),id=Number(ins.meta.last_row_id);let discovered=0,processed=0,skipped=0,failed=0,queued=0;const errors:string[]=[];try{const cp=await checkpoint(e,'last_successful_scan'),init=await checkpoint(e,'system_initialized_at'),since=cp?new Date(cp):init?new Date(init):new Date(Date.now()-num(e.LOOKBACK_MINUTES,90)*60000),found=await sources(e,since),all=found.items.sort((a,b)=>a.modifiedAt.localeCompare(b.modifiedAt)),items=all.slice(0,num(e.MAX_ITEMS_PER_RUN,3));discovered=all.length;queued=Math.max(0,all.length-items.length);for(const s of items)try{(await handle(e,found.token,s))==='processed'?processed++:skipped++}catch(x){failed++;errors.push(`${s.sourceType}:${s.id}: ${x instanceof Error?x.message:String(x)}`)}if(!failed)await setCheckpoint(e,'last_successful_scan',queued&&items.length?items[items.length-1].modifiedAt:started);const status=failed?'PARTIAL':'SUCCESS';await e.DB.prepare('UPDATE runs SET finished_at=?,status=?,discovered=?,processed=?,skipped=?,failed=?,detail=? WHERE id=?').bind(now(),status,discovered,processed,skipped,failed,JSON.stringify({queued,errors}),id).run();if(failed)await alert(e,`⚠️ *Agente Cliente APD - ${status}*\nEjecución ${id}\nFallidos ${failed}\n${errors.join(' | ')}`);return{id,status,discovered,processed,skipped,failed,queued,dryRun:yes(e.DRY_RUN,true),errors}}catch(x){const m=x instanceof Error?x.message:String(x);await e.DB.prepare("UPDATE runs SET finished_at=?,status='FAILED',detail=? WHERE id=?").bind(now(),m,id).run();await alert(e,`🚨 *Agente Cliente APD - FAILED*\n${m}`);throw x}}